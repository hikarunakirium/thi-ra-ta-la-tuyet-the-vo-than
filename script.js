let book; let rendition; let pdfDoc = null; let isPdfMode = false; let currentLocationCfi = ""; let currentChapterName = "Đang đọc..."; let GLOBAL_PREFIX = "reader_global_"; let BOOK_PREFIX = "reader_book_"; let savedBookmarks = []; window.onload = () => { loadSettings(); }; function handleFileUpload(event) { const file = event.target.files[0]; if (!file) return; const ext = file.name.split('.').pop().toLowerCase(); isPdfMode = (ext === 'pdf'); BOOK_PREFIX = (isPdfMode ? "pdf_" : "epub_") + file.name.replace(/[^a-zA-Z0-9]/g, '') + "_"; savedBookmarks = JSON.parse(localStorage.getItem(BOOK_PREFIX + 'bookmarks') || '[]'); document.getElementById('upload-screen').style.display = 'none'; document.getElementById("reader-container").innerHTML = ""; if (book) { book.destroy(); book = null; } pdfDoc = null; const reader = new FileReader(); reader.onerror = function() { alert("❌ Lỗi: Không thể đọc file. Vui lòng thử lại."); document.getElementById('upload-screen').style.display = 'flex'; }; reader.onload = function(e) { try { if (isPdfMode) { initPdfReader(new Uint8Array(e.target.result)); } else { initEpubReader(e.target.result); } } catch(err) { console.error("Lỗi xử lý file:", err); alert("❌ Lỗi: Không thể xử lý file này. Kiểm tra file có hợp lệ không."); document.getElementById('upload-screen').style.display = 'flex'; } }; reader.readAsArrayBuffer(file); } function initEpubReader(bookData) { try { book = ePub(bookData); rendition = book.renderTo("reader-container", { manager: "continuous", flow: "scrolled-doc", width: "100%", height: "auto", snap: false, allowPopups: true }); rendition.hooks.content.register(function(contents, view) { contents.document.body.style.WebkitOverflowScrolling = 'touch'; contents.document.documentElement.style.overflowY = 'auto'; contents.document.body.style.padding = '20px'; contents.document.body.style.lineHeight = '1.8'; }); registerThemes(); applySettings(false); renderBookmarks(); const lastLocation = localStorage.getItem(BOOK_PREFIX + 'lastRead'); book.ready.then(() => { if (lastLocation) { rendition.display(lastLocation).catch(err => { console.warn("Không thể load vị trí trước, hiển thị trang đầu:", err); rendition.display(); }); } else { rendition.display(); } }); rendition.on("relocated", (location) => { if(location && location.start) { currentLocationCfi = location.start.cfi; localStorage.setItem(BOOK_PREFIX + 'lastRead', currentLocationCfi); const cleanHref = location.start.href.split('#')[0]; let navItem = book.navigation.get(cleanHref) || book.navigation.get(location.start.href); if(navItem) { currentChapterName = navItem.label; highlightSidebarItem(currentChapterName); } } }); book.loaded.navigation.then((nav) => { const list = document.getElementById('chapter-list'); if(nav && nav.length > 0) { list.innerHTML = nav.map(chapter => `<a class=\"chapter-link\" onclick=\"goTo('${chapter.href}')\">${chapter.label}</a>`).join(""); } else { list.innerHTML = "<div style='font-size:13px; color:gray;'>Không có chương nào.</div>"; } }).catch(err => { console.error("Lỗi load navigation:", err); }); } catch(err) { console.error("Lỗi khởi tạo EPUB:", err); alert("❌ Lỗi: Không thể đọc file EPUB này."); document.getElementById('upload-screen').style.display = 'flex'; } } async function initPdfReader(pdfData) { try { if (typeof pdfjsLib !== 'undefined') { pdfjsLib.GlobalWorkerOptions.workerSrc = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js'; } else { throw new Error("PDF.js library not loaded"); } const pdfLoadingTask = pdfjsLib.getDocument({data: pdfData}); pdfDoc = await pdfLoadingTask.promise; const container = document.getElementById("reader-container"); const list = document.getElementById('chapter-list'); let tocHtml = ""; const totalPages = pdfDoc.numPages; for(let i = 1; i <= totalPages; i += 5) { tocHtml += `<a class=\"chapter-link\" onclick=\"goTo('${i}')\">Trang ${i}</a>`; } if(totalPages % 5 !== 1) { tocHtml += `<a class=\"chapter-link\" onclick=\"goTo('${totalPages}')\">Trang ${totalPages} (Cuối)</a>`; } list.innerHTML = tocHtml; for(let i = 1; i <= totalPages; i++) { let pageDiv = document.createElement("div"); pageDiv.id = `pdf-page-${i}`; pageDiv.className = "pdf-page-container"; pageDiv.dataset.page = i; pageDiv.innerHTML = `<div style=\"text-align:center; color:#999; padding:20px;\">Trang ${i} đang tải...</div>`; container.appendChild(pageDiv); } renderBookmarks(); const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if(entry.isIntersecting) { const pageNum = parseInt(entry.target.dataset.page); renderPdfPage(pageNum, entry.target); currentLocationCfi = pageNum.toString(); currentChapterName = `Trang ${pageNum}`; localStorage.setItem(BOOK_PREFIX + 'lastRead', currentLocationCfi); const targetPageText = `Trang ${Math.ceil(pageNum / 5) * 5 - 4}`; highlightSidebarItem(targetPageText); } }); }, { rootMargin: "500px 0px" }); document.querySelectorAll('.pdf-page-container').forEach(el => observer.observe(el)); const lastRead = localStorage.getItem(BOOK_PREFIX + 'lastRead'); if (lastRead) { setTimeout(() => goTo(lastRead), 500); } } catch (err) { console.error("Lỗi PDF:", err); alert("❌ Lỗi khi đọc file PDF: " + err.message); document.getElementById('upload-screen').style.display = 'flex'; } } async function renderPdfPage(pageNum, container) { if(container.dataset.rendered === 'true') return; try { const page = await pdfDoc.getPage(pageNum); const screenWidth = document.getElementById("content-area").clientWidth - 20; const initialViewport = page.getViewport({scale: 1}); const scale = Math.min(2, screenWidth / initialViewport.width); const viewport = page.getViewport({scale: scale}); const canvas = document.createElement("canvas"); const ctx = canvas.getContext("2d"); canvas.height = viewport.height; canvas.width = viewport.width; canvas.className = "pdf-canvas"; container.innerHTML = ""; container.appendChild(canvas); await page.render({canvasContext: ctx, viewport: viewport}).promise; container.dataset.rendered = 'true'; } catch(err) { console.error(`Lỗi render trang ${pageNum}:`, err); container.innerHTML = `<div style=\"text-align:center; color:red; padding:20px;\">❌ Lỗi tải trang ${pageNum}</div>`; } } function highlightSidebarItem(textMatch) { document.querySelectorAll('.chapter-link').forEach(el => el.classList.remove('active')); const links = Array.from(document.querySelectorAll('.chapter-link')); const activeLink = links.find(el => el.textContent.trim().includes(textMatch.trim())); if (activeLink) activeLink.classList.add('active'); } function goTo(target) { if (isPdfMode) { const el = document.getElementById(`pdf-page-${target}`); if(el) { document.getElementById("content-area").scrollTo({ top: el.offsetTop - 50, behavior: 'smooth' }); } } else { if(rendition) { rendition.display(target).catch(err => { console.warn("Lỗi hiển thị:", err); }); } } closeAllPanels(); } function registerThemes() { if(!rendition) return; rendition.themes.register("dark", { "body": { "background": "#121212", "color": "#d4d4d4" }, "a": { "color": "#375a7f" } }); rendition.themes.register("light", { "body": { "background": "#f8f9fa", "color": "#212529" }, "a": { "color": "#007bff" } }); } function loadSettings() { if (window.matchMedia('(prefers-color-scheme: dark)').matches && !localStorage.getItem(GLOBAL_PREFIX + 'theme')) { document.body.setAttribute('data-theme', 'dark'); } else { const theme = localStorage.getItem(GLOBAL_PREFIX + 'theme'); if (theme) document.body.setAttribute('data-theme', theme); } const fFamily = localStorage.getItem(GLOBAL_PREFIX + 'font') || "'Times New Roman', serif"; const fSize = localStorage.getItem(GLOBAL_PREFIX + 'size') || "21"; const fLine = localStorage.getItem(GLOBAL_PREFIX + 'line') || "1.8"; document.getElementById('fontFamily').value = fFamily; document.getElementById('fontSize').value = fSize; document.getElementById('lineHeight').value = fLine; applySettings(false); } function applySettings(save = true) { const fFamily = document.getElementById('fontFamily').value; const fSize = document.getElementById('fontSize').value; const fLine = document.getElementById('lineHeight').value; const isDark = document.body.getAttribute('data-theme') === 'dark'; document.getElementById('fontSizeVal').innerText = fSize; document.getElementById('lineHeightVal').innerText = fLine; if(!isPdfMode && rendition) { try { rendition.themes.font(fFamily); rendition.themes.fontSize(fSize + "px"); rendition.themes.override("line-height", fLine); rendition.themes.select(isDark ? "dark" : "light"); } catch(err) { console.warn("Lỗi áp dụng theme:", err); } } if (save) { localStorage.setItem(GLOBAL_PREFIX + 'font', fFamily); localStorage.setItem(GLOBAL_PREFIX + 'size', fSize); localStorage.setItem(GLOBAL_PREFIX + 'line', fLine); } } function toggleTheme() { const isDark = document.body.getAttribute('data-theme') === 'dark'; document.body.setAttribute('data-theme', isDark ? 'light' : 'dark'); localStorage.setItem(GLOBAL_PREFIX + 'theme', isDark ? 'light' : 'dark'); applySettings(true); } function addBookmark() { if (!currentLocationCfi) return; const exists = savedBookmarks.find(b => b.cfi === currentLocationCfi); if (!exists) { savedBookmarks.push({ cfi: currentLocationCfi, name: currentChapterName }); localStorage.setItem(BOOK_PREFIX + 'bookmarks', JSON.stringify(savedBookmarks)); renderBookmarks(); alert(`🔖 Đã lưu:
${currentChapterName}`); } else { alert("Bạn đã lưu vị trí này rồi."); } } function removeBookmark(event, index) { event.stopPropagation(); savedBookmarks.splice(index, 1); localStorage.setItem(BOOK_PREFIX + 'bookmarks', JSON.stringify(savedBookmarks)); renderBookmarks(); } function renderBookmarks() { const list = document.getElementById('bookmark-list'); if(savedBookmarks.length === 0) { list.innerHTML = "<div style='font-size:13px; color:gray;'>Chưa có bookmark nào.</div>"; return; } list.innerHTML = savedBookmarks.map((bm, idx) => ` <div style=\"display:flex; align-items:center; gap:5px; margin-bottom:4px; padding-bottom: 4px; border-bottom: 1px dashed var(--border);\"> <a class=\"chapter-link\" style=\"flex:1; margin:0; font-size:14px; border:none;\" onclick=\"goTo('${bm.cfi}')\">🔖 ${bm.name}</a> <button onclick=\"removeBookmark(event, ${idx})\" style=\"padding:6px 10px; border:none; background:var(--bg); color:red; border-radius:4px;\">✕</button> </div> `).join(""); } function exportData() { const data = { lastRead: localStorage.getItem(BOOK_PREFIX + 'lastRead'), bookmarks: localStorage.getItem(BOOK_PREFIX + 'bookmarks'), theme: localStorage.getItem(GLOBAL_PREFIX + 'theme'), font: localStorage.getItem(GLOBAL_PREFIX + 'font'), size: localStorage.getItem(GLOBAL_PREFIX + 'size'), line: localStorage.getItem(GLOBAL_PREFIX + 'line') }; const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'Backup_Reader.json'; a.click(); URL.revokeObjectURL(url); } function importData(event) { const file = event.target.files[0]; if (!file) return; const reader = new FileReader(); reader.onload = (e) => { try { const data = JSON.parse(e.target.result); if (data.lastRead) localStorage.setItem(BOOK_PREFIX + 'lastRead', data.lastRead); if (data.bookmarks) localStorage.setItem(BOOK_PREFIX + 'bookmarks', data.bookmarks); if (data.theme) localStorage.setItem(GLOBAL_PREFIX + 'theme', data.theme); if (data.font) localStorage.setItem(GLOBAL_PREFIX + 'font', data.font); if (data.size) localStorage.setItem(GLOBAL_PREFIX + 'size', data.size); if (data.line) localStorage.setItem(GLOBAL_PREFIX + 'line', data.line); alert("✅ Phục hồi dữ liệu thành công!"); loadSettings(); renderBookmarks(); if(data.lastRead) goTo(data.lastRead); } catch (err) { alert("❌ Lỗi: File dữ liệu không hợp lệ."); } }; reader.readAsText(file); } function toggleSidebar() { document.getElementById('sidebar').classList.toggle('open'); document.getElementById('settings-panel').classList.remove('open'); checkOverlay(); if (document.getElementById('sidebar').classList.contains('open')) { const activeItem = document.querySelector('.chapter-link.active'); if(activeItem) activeItem.scrollIntoView({ block: 'center' }); } } function toggleSettings() { document.getElementById('settings-panel').classList.toggle('open'); document.getElementById('sidebar').classList.remove('open'); checkOverlay(); } function closeAllPanels() { document.getElementById('sidebar').classList.remove('open'); document.getElementById('settings-panel').classList.remove('open'); document.getElementById('overlay').classList.remove('show'); } function checkOverlay() { const isAnyOpen = document.getElementById('sidebar').classList.contains('open') || document.getElementById('settings-panel').classList.contains('open'); document.getElementById('overlay').classList.toggle('show', isAnyOpen); }